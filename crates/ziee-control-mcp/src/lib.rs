//! `ziee-control-mcp` — the LLM-control surface (build-DB-free).
//!
//! Future contents: the app's OpenAPI exposed as MCP tool-dispatch
//! (list/describe/invoke_capability), forwarded-JWT loopback re-auth, secret
//! denylist, mutation-approval, and permission filtering — how the ziee
//! companion drives a new app.

pub fn _placeholder() {}
