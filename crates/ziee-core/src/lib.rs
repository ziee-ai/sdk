//! `ziee-core` — foundation crate (build-DB-free).
//!
//! Future contents: `AppError`/`ApiResult`, shared macros, base app-state
//! globals (configurable app-name), and `ServerConfig`.

pub fn _placeholder() {}
