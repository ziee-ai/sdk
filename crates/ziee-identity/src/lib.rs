//! `ziee-identity` — identity ABSTRACTIONS (build-DB-free).
//!
//! Future contents: the `Principal`/identity trait, the JWT-verify interface,
//! `PermissionCheck`/`PermissionList` traits with wildcard/`is_admin` RBAC eval,
//! and refresh-token verification. No concrete tables — identity is pluggable.

pub fn _placeholder() {}
