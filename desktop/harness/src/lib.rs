//! `ziee-desktop-harness` — the desktop shell (future).
//!
//! Future contents: the Tauri shell embedding a framework server + embedded
//! Postgres + the single-user auto-login auth strategy (permanent session),
//! factored out of the app's `desktop/tauri`.

pub fn _placeholder() {}
