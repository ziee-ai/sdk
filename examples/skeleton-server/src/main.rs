//! `skeleton-server` — the permanent, app-agnostic second-consumer guard.
//!
//! A minimal SDK-only server that must keep compiling against the framework
//! with no app-specific code, proving the platform layer stays app-agnostic.

fn main() {
    println!("skeleton");
}
