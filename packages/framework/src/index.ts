// @ziee/framework — domain-agnostic UI runtime (future):
// core/store-kit, stores/createStoreProxy, module-system, events, overrides,
// router module, sync client, and the ApiClient runtime.
export const _placeholder = true;
